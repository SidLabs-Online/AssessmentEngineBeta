# Root environment reference for local development.
# This file covers all env vars currently used by the checked-in codebase.
# Copy to `.env` at the repo root before running the app.

# Frontend
VITE_API_BASE_URL=http://localhost:5001

# Backend
PORT=5001
NODE_ENV=development
CLIENT_ORIGIN=http://localhost:5173

# MongoDB connection string used by the submission storage layer.
MONGODB_URI=mongodb+srv://fullstackdeveloper27:1aDTBKDCFmNQiJi5@cluster0.cbsrb.mongodb.net/assessment-engine?retryWrites=true&w=majority&appName=Cluster0

# JWT secret used for signed auth cookies.
JWT_SECRET=replace-with-a-long-random-string

# Admin credentials used for initial secure evaluator seeding.
ADMIN_EMAIL=evaluator@sidlabs.net
ADMIN_INITIAL_PASSWORD=replace-with-a-strong-admin-password

# Demo candidate credentials used by the current auth foundation.
DEMO_USER_EMAIL=candidate@gmail.com
DEMO_USER_PASSWORD=Candidate@2026
